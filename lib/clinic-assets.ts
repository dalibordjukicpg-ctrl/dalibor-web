/** Podrazumijevani logo (transparentan PNG); upload u adminu ga nadjačava. */
export const DEFAULT_HEADER_LOGO = "/logo/simple-solutioning-transparent.png";

/** Kredit u podnožju — isti link kao Motrenko / dalibor projekat. */
export const CRAFTED_BY_HREF = "https://www.computer-doctor.me";

export const HEADER_LOGO_PIXEL_WIDTH = 974;
export const HEADER_LOGO_PIXEL_HEIGHT = 339;

/** Podrazumijevana hero pozadina — premium pejzaž / eksterijer. */
export const FALLBACK_HERO_IMAGE_REL = "/hero/landscape-hero.jpg";

/** Portret u sekciji „Upoznajte dizajnere“ (O nama) na početnoj. */
export const FALLBACK_ABOUT_PORTRAIT_REL = "/about/designer-portrait.jpg";

/** Unutrašnje stranice — ista estetika kao početna. */
export const CLINIC_PAGE_HERO_BG = FALLBACK_HERO_IMAGE_REL;

/** Zastarjeli aliasi — ostaju radi kompatibilnosti importa. */
export const FALLBACK_HERO_VIDEO_PATH_REL = FALLBACK_HERO_IMAGE_REL;
export const FALLBACK_HERO_VIDEO_POSTER_REL = FALLBACK_HERO_IMAGE_REL;
export const FALLBACK_HERO_VIDEO_MOBILE_REL = FALLBACK_HERO_IMAGE_REL;
